/**
 * Letters V2 Types - Unified Export
 *
 * Import all types from one place:
 * ```typescript
 * import type { LetterV2, EmailRenderResult, ImageName } from '@/modules/letters-v2/types';
 * ```
 */

export type {
  LetterV2,
  RenderOptions,
  EmailAttachment,
  EmailRenderResult,
  ImageMap,
  ParsedLetter,
  ImageName,
} from './letters-v2.types';
